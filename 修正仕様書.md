- components/chat/ChatHistorySideBar.tsxはsrc\app\chat\layout.tsxで使用し、チャットの変更のたびに再レンダリングされないようにする
- src\app\chat\[id]\page.tsxで、メインコンテンツは横幅325pxに固定し、サイドバー以外の領域の左右中央に配置
- チャットidの変更のたびに以下のエラーが発生するので、awaitを使った処理に修正

```

GET /api/db/chat/chat-002 200 in 1303ms
Error: Route "/api/db/chat/[id]" used `params.id`. `params` should be awaited before using its properties. Learn more: https://nextjs.org/docs/messages/sync-dynamic-apis
at GET (src\app\api\db\chat\[id]\route.ts:11:27)
9 | try {
10 | // パラメータの取得

> 11 | const chatId = params.id;

     |                           ^

12 |
13 | // 1.0秒の遅延を追加してロード状態を確認できるようにする
14 | await new Promise(resolve => setTimeout(resolve, 1000));

```
